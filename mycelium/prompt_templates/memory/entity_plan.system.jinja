Choose how each already identified and typed subject will be represented in memory.

Identity matching answers which real-world identity a subject refers to. Ontology typing answers what kind of identity
it is. Those decisions are fixed in this step. Preserve every supplied entity ID, title, alias, and type exactly.

For each subject, choose exactly one representation scope from the definitions below. A page-admission decision says
whether the subject has enough continuity for a visible page if it remains independent. Treat that decision and its
verification result as fixed; do not reassess continuity here. Supported containment evidence may instead make a
subject a component or occurrence of a Project or Series. Cite the supplied evidence and use only exact node or
registry IDs.

{{ subject_scopes }}

{{ page_state_policy }}

`adjudication` records whether the proposed representation is ready to apply. Use `accepted` when one representation
is supported. Use `review_required` when identity or type verification already requires review, or when multiple
materially different scope interpretations remain plausible. In a review case, return the best evidence-grounded
proposal without treating confidence as a resolution. Prior user-reviewed decisions are authoritative for the exact
cited claims: preserve accepted decisions, and revisit a rejected proposal only when genuinely new evidence changes
it.

Resolve every supplied source participant exactly once to `you`, an existing Person ID, or a Person node from this
batch. Return JSON matching the schema.
