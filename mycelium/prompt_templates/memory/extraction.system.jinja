When a reply accepts or refuses a proposal, preserve that decision in the assertion itself. A bare prediction that
an action will or will not happen can lose the speaker's act of agreeing or declining. Interpret the reply with
its cited proposal, and retain both the decision and the proposal's specific scope without inventing a reason.

Choose the supporting segment_ids and any context_segment_ids before writing the assertion. Fill facets with
its stated temporal limits, then write self-contained text retaining those limits. Put citations only in citation
fields: a segment ID or phrase such as "mentioned earlier" cannot replace the actual details in readable text.

For a brief reply resolved through cited earlier context, state the complete proposition being accepted, refused,
or qualified. Carry that proposition's time, location, and other limiting details into the claim text. Citing the
context alone is not enough: downstream readers must understand the assertion without opening its source.
Preserve the reply's actual decision or uncertainty; do not inherit an assistant's proposed commitment unless
this speaker accepts it. Retain the contextual scope for refusals as well as acceptances.

Return temporal_status deliberately for every claim: past for a completed occurrence or ended state, current for an
ongoing state at the source's time, future for a prospective action, recurring for a repeated habit, atemporal for
knowledge without a time-bound state, and unknown only when the evidence cannot establish this distinction.
A remembered past occurrence remains past even though remembering it is useful now. Classify each assertion's own
scope, not the present tense of the speaker reporting it. Preserve relative wording and its source context.
Before writing a claim, identify exactly what the cited speaker asserts: choosing or intending an action is not
performing it. Retain the direction, outcome, and qualifications actually stated; do not complete a plausible story.
Descriptions of a concrete experience's setting, effect, or distinctive details are source-supported assertions too.
Assess those assertions separately from surrounding encouragement or generic discussion.

Turn the {{ source_type }} segments into durable, atomic stored claims.

A stored claim is one source-supported assertion that can be evaluated and updated independently. Write each claim as
a standalone third-person subject-predicate sentence with explicit names. Resolve first- and second-person references
from the supplied source context. Preserve exact names, dates, relative-time phrases, locations, quantities, reasons,
and ownership. Cite only segment IDs that directly support the claim.

Preserve the assertion's strength in the readable sentence itself: possibility, consideration, desire, intention,
decision, and completed action are not interchangeable. Do not upgrade a tentative possibility into an unqualified
plan, or weaken an explicit commitment into mere consideration. Preserve negation and any condition on an intention
in the same statement as the action it qualifies. An unfulfilled condition is not an established fact.
Keep attribution when a speaker reports someone else's belief or possibility; reporting it is not independent
confirmation. A claim category or confidence score cannot substitute for these words. Confidence measures how well
the memory is supported, not how certain the described future event is. Tentative assertions can be durable memories.

{% if source_type != "tool_observation" %}
Durable memory does not mean a permanent condition or an unusually important event. A concrete reported experience,
action, observation, or change in condition can remain useful as dated history after the condition ends. Preserve
its specific details and time scope; do not discard it merely because it is ordinary, descriptive, or temporary.
Conversely, generic encouragement, politeness, and advice without a concrete adopted assertion do not establish
the recipient's traits, commitments, or activities. Distinguish evidence about someone's life from conversation
that merely supports or encourages them.
{% endif %}

The source-participant roster is an unordered attendance list. Speaker labels identify who spoke; the words and turn
context identify who was addressed. Use those together to turn first-, second-, and plural-person references into
explicit names. A two-person roster resolves “both of us” to those two people. With a larger roster, use an explicit
name or clear turn context to identify the other member.

Policy for this source:
{{ source_policy }}

For each claim:
- claim_type ({{ claim_types | join('/') }}) chooses one declared category.
- `predicate` is a short open label for what the sentence asserts.
- `about` lists the named identities used by later identity resolution and routing:
{{ subject_policy }}
- `slot` optionally names a replaceable state whose newest supported value can supersede an older value. Leave it
  empty when the claim is cumulative history rather than one current value.
- `facets` stores structured details such as `when`, `deadline`, or `inference_basis` without replacing the readable
  claim sentence.

`evidence_modality` records how the evidence was observed: `speech` for spoken or written conversation, `visual` for
visible evidence, `tool` for a tool observation, and `mixed` only when multiple channels directly support the same
claim. `evidence_type` records whether the assertion was directly stated (`explicit`) or follows with high certainty
from the cited evidence (`inferred`). An inferred claim includes `facets.inference_basis` and confidence no greater
than 0.7.

Temporal details describe when the remembered event or state happens. Put time wording stated in the cited words in
`facets.when`, and due-date wording stated there in `facets.deadline`. Preserve relative phrases such as “by Friday”
or “in three days.” The storage layer resolves a relative phrase from the cited evidence when its source time is
unambiguous. When cited evidence spans multiple source times, `temporal_anchor_segment_id` identifies the cited
segment containing the phrase.

Encode a continuing role or responsibility between a person and a Project as `claim_type=relationship` and
`predicate=project_role`, with the person as subject and the Project as owner. A one-off action, attendance, or
incidental contribution is not a continuing role.

Return claims and a source_only list for the remaining new segments not cited by any claim.
Write claims first. After their citations are complete, account for only the uncited remainder in source_only.
A cited new segment is already accounted for; do not classify it a second time. Use source_only when it only carries
the conversation forward, repeats evidence exactly, or contains a suggestion or external content nobody adopted
or made relevant. Explain each source_only entry from the evidence. Durability, not subjective importance, determines
admission. Every new segment must either be cited by at least one claim OR appear exactly once in source_only,
never both. Omitting a segment is invalid; it does not mean source_only. An empty claims list
is valid when every segment is source_only. Do not turn an assistant suggestion into a user decision without
supporting user evidence. Earlier context resolves references but is not a new extraction target.
When resolving a pronoun, acceptance, or refusal using earlier context, the resulting claim MUST cite the earlier
segment supplying the subject/action/details in context_segment_ids. Cite the new assertion in segment_ids.
Do not put those earlier evidence IDs only in a reason or inference_basis; the structured citations must include them.

A question or request can also contain durable assertions. Extract its supported self-reported
roles, experiences, preferences, interests, and goals even when the same segment asks for advice. Those assertions
do not need a subsequent decision or action to qualify. Evaluate the assertions within the segment, not merely
whether the whole turn is phrased as a question. A general informational question alone does not establish a
personal preference or plan. Do not promote a proposed assistant action into an accepted user commitment.
