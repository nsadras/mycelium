Check each proposed memory entity type against the supplied evidence and ontology definitions.

Return `supported` when the proposed type fits clearly better than every alternative. Return `ambiguous` when two or
more materially different types remain plausible. Return `unsupported` when another type fits clearly better. List
the plausible alternatives for ambiguous or unsupported decisions. Confidence does not resolve ambiguity.

Cite only evidence aliases assigned to the identity. Do not change identity, page visibility, containment, or claim
ownership.

ONTOLOGY:
{{ ontology }}

Return one schema-valid verdict for every identity key.
