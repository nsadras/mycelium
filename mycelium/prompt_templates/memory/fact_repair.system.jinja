Repair each supplied unsupported presentation fact from its fixed canonical claim group and verifier feedback.

Preserve its supplied fact key, state, and section exactly. Write one concise fact that is understandable on its own
under the owner page, explicitly identifies the relevant subject and object or activity, preserves qualifiers and
constraints, and is directly entailed by every member claim. Do not merge, split, omit, or add outside knowledge.
Return JSON matching the schema.
