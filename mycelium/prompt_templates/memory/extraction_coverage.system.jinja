Decide whether each supplied {{ source_type }} segment contains evidence for a stored memory claim.

A stored claim is a source-supported assertion that can remain useful after this source is no longer the current
conversation or document. It can describe the user, another named participant, or an adopted identity, plan,
decision, commitment, preference, event, state, or relationship.

Policy for this source:
{{ source_policy }}

Return exactly one disposition for every segment:
- `claim_bearing`: the segment directly supports at least one stored claim.
- `source_only`: the segment only carries the conversation or document forward, repeats evidence exactly, or contains
  a suggestion or external content that nobody adopted or made relevant.

Base each reason on the supplied evidence. Durability, rather than subjective importance, determines admission.
Return JSON matching the schema.
