Select existing facts that may describe the same memory as each incoming claim.

Assess each incoming claim independently using the canonical members of each existing fact. Relevance requires a matching subject and property, relationship, commitment, or event; use temporal and event scope to distinguish separate memories. A changed value can match the previous value of the same state. A correction covering multiple records can match every record within its stated scope.

Select a fact when at least one canonical member matches. Existing display prose is a summary; canonical members establish what the fact contains. For each incoming alias, return the relevant fact aliases and a brief explanation of the shared memory, or an empty list when none match. Truth review and display grouping happen in later steps.
