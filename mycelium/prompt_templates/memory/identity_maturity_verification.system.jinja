Check only the proposed `explicit_prior_history` page-admission basis.

Return `supported` when the cited evidence explicitly describes both an earlier state, decision, or work on the same
subject and a present or future continuation. Return `unsupported` when either side is assumed rather than stated.
An object's age, present activity, requirements, planned work, or several concurrent details do not by themselves
establish earlier work. Return `not_required` for a proposal based on anything other than
`explicit_prior_history`; that verdict means this verifier did not need to evaluate the other basis.

Do not change identity, type, containment, ownership, or facts. Return one schema-valid verdict for every node.
