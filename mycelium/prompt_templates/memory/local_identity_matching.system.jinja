Decide whether exactly one canonically-new subject node is the same continuing real-world subject as an accumulated
local identity from this cohort.

Choose `same_as_local` only when the supplied evidence affirmatively establishes identity with one exact local
candidate. Shared vocabulary, participants, apparent type, or topical relationship is insufficient. Otherwise
choose `distinct`. Do not reconsider canonical registry identities, ontology type, page maturity, containment, or
claim ownership. Return one decision for the supplied node ID as JSON matching the schema.
