Select prior presentation facts that are materially relevant to resolving each incoming canonical claim.

Selection is high-recall candidate generation only: do not decide contradiction, supersession, grouping, section, or
display wording. Select a prior fact when it may express the same durable state, preference, commitment,
relationship, plan, event, or truth predecessor as the incoming claim. Shared topic or vocabulary alone is
insufficient. Return an empty candidate list when no supplied prior fact is materially relevant. Cite the semantic
connection in the reason and return JSON matching the schema.
